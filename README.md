# InfoLED
Scripts to operate on an RaspberryPI with an attached LED matrix

## Features
- Show the current temperature
- Uses an emulator for local development

## Setup
- Adapt config.ini to your needs
- Get an API key for openweathermap.org and set to the `WEATHER_API_KEY` environment variable.

![emulator](readme_images/emulator.png)
